apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - route.yaml                # new resource to expose the prediction service via an ocp Route

patches:
  - path: deployment.yaml     # drop GKE nodeSelector + restricted-v2 securityContext
  - path: service.yaml        # LoadBalancer -> ClusterIP (sits behind the Route)
